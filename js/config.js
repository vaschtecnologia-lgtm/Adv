// Configuration for the Google AI Studio Quickstart
const CONFIG = {
  // The Gemini API Key can be configured here, or entered in the web UI.
  // It is safer to enter it in the web UI or configure it as an environment variable.
  API_KEY: "",
  DEFAULT_MODEL: "gemini-2.5-flash",
};
